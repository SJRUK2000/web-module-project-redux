const initalState = {
    favorites: [],
    displayFavorites: true 
}

const favoritesReducer = (state = initalState, action) => {
    switch(action.type){
        default:
            return(state);
    }
}

export default favoritesReducer;
